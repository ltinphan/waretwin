# Runtime 直接相依（範圍鎖定 minor）。完整可重現的版本在 requirements.lock（Render / Docker 使用）。
fastapi>=0.115,<0.142
uvicorn[standard]>=0.30,<0.47
pydantic>=2.7,<3
websockets>=12,<18
openai>=1.40,<4
python-dotenv>=1.0,<2
shapely==2.1.2
